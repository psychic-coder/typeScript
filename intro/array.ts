 const superHoes:string[]=[]
 superHoes.push("ironwoman")
//these are the way we define an array
 const hoePower:number[]=[]
 hoePower.push(2);

//we can also define an array in this way
 const hoeDance:Array<Number>=[]
 



 // we created a custom data type and created an array with that and pushed it in the array
type User={
    name:string
    isActive:boolean
}
const allUsers:User[]=[]
allUsers.push({name:"bro",isActive:false})


//2d arrays
const MLModels:number[][]=[
    [233,44,55],
    [323,44,22,23]
]



 export{}
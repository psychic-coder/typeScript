


let tuser:[string,number,boolean]
//in tuples we cannot eben change the order of the data , the dataorder has to be the same
tuser=["hw",23,true]


let rgb:[number,number,number]
rgb=[32,2312,12]


// we can even use the tuple in type
type User=[number,string]
const newUser:User=[12,"DAcae"]

//we can even change the value
newUser[1]="2e.com"
// as tuples is an array
//we can even use the array methods

//the below error is removed
// newUser.push(true)










export{}